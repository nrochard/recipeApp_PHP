<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- SCRIPT VUE -->
    <script defer src="https://unpkg.com/vue@next"></script>
    <!-- SCRIPT JS -->
    <script defer src="/js/app.js"></script>
    <!-- CSS -->
    <link rel="stylesheet" href="css/style.css">
    <!-- BOOSTRAP -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ho+j7jyWK8fNQe+A12Hb8AhRq26LrZ/JpcUGGOn+Y7RsweNrtN/tE3MoK7ZeZDyx" crossorigin="anonymous"></script>
    <title>Cuisine moi !</title>
</head>
<body >

    <div id="app" class="container mb-5">
        <h1 class="mt-3">Cuisine moi !</h1>
        <h2 class="sous-titre">La bonne cuisine est honnête, sincère et simple.</h2>
        <div class="row mt-5 container_form">
            <div class="col-md-6">
                <img src="/img/cook.svg" class="svg_img"> 
            </div>
            <div class="col-md-6">
                {% include 'partials/form.twig' %}
            </div>
        </div>

        <h2 class="mt-5 text-center" v-if="recipes">Toutes les recettes</h2>
         <h4 class="text-center" v-if="recipes.length == 1">{{ recipes.length }} recette</h3>
        <h4 class="text-center" v-if="recipes.length > 1">{{ recipes.length }} recettes</h3>

        <div class="card mt-3" style="width: 100%;" v-for="recipe in recipes">
        <div class="card-body">
            <h5 class="card-title">{{ recipe.name }}</h5>
            <div class="container_icon mt-2 mb-2" >
                <div class="col-md-6 flex_icon">
                    <img src="/img/clock.svg" class="svg_icon">
                    <p>{{ recipe.people }} personnes</p>
                </div>
                <div class="col-md-6 flex_icon">
                    <img src="/img/group.svg" class="svg_icon">
                    <p>{{ recipe.time }} minutes</p>
                </div>
            </div>
            <p class="card-text">{{ recipe.ingredients }}</p>
            <p class="card-text">{{ recipe.preparation }} </p>
            <a href="#" class="btn btn-warning" @click.prevent="deleteRecipe(recipe.id)" >Supprimer</a>
        </div>
        </div>
    </div>

</body>
</html>